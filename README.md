# DX-146 screenshots

Image assets referenced from the body of PR #44 (`feat(DX-146): first-run onboarding for the
skills catalogue`). They live on their own branch so the binaries never reach `main`, while
still being served over `raw.githubusercontent.com` for anyone who can read the repo.

**Do not merge this branch and do not open a pull request from it.** It shares no history with
`main` — it is an orphan branch holding nothing but these PNGs.

Delete it once PR #44 is merged and its description no longer needs to render.
